#!/bin/bash
# check-prs — verify a pull request is registered for every repo this node
# execution pushed commits to.
#
# Usage: check-prs
#
# Reads /workspace/config.json's repos[] (id, name, local_path). For each
# repo, determines whether its current branch was pushed to origin this run
# (via `git ls-remote`, not the local upstream-tracking ref — see the header
# comment in entrypoint.sh's PR-verification block for why) AND carries
# commits the repo's default branch does not (see branch_adds_commits below:
# a branch at or behind the default branch's tip can have no pull request, so
# it is not gated). For every repo
# that was pushed but has no pull request registered with the ChorusKube API
# for this run (checked via this node execution's own scoped read of the
# whole run's PR set — see RunPullRequestService#getPullRequestsForNodeExecution
# — not just PRs this node execution itself registered), prints
# "<repo-name>: no PR registered".
#
# Unlike check-decision's single-value "(none)" convention, check-prs's
# output is a variable-length list (zero or more lines) — callers MUST branch
# on the exit status, not on the text of stdout.
#
# Environment:
#   API_SERVER_URL      — Base URL of the ChorusKube API server
#   RUN_ID               — Workflow run UUID
#   NODE_EXECUTION_ID    — Node execution UUID
#   JOB_SECRET           — Bearer token for authentication
#
# Examples:
#   # Fail the node if any pushed repo is missing a registered PR
#   if ! check-prs; then echo "PR(s) missing"; fi
#
# Exit codes:
#   0  Every repo this node pushed commits to has a registered PR (including
#      zero such repos)
#   1  One or more pushed repos are missing a registered PR, a required
#      environment variable is unset, the API call failed, or a repo's
#      remote could not be reached (see "could not reach origin" below)
set -euo pipefail

if [ "${1:-}" = "--help" ] || [ "${1:-}" = "-h" ]; then
    sed -n '2,/^set -euo pipefail$/{ /^set -euo pipefail$/d; s/^# \?//; p }' "$0"
    exit 0
fi

if [ -z "${API_SERVER_URL:-}" ]; then
    echo "ERROR: API_SERVER_URL not set. This script must be run inside an agent pod." >&2
    exit 1
fi

if [ -z "${RUN_ID:-}" ] || [ -z "${NODE_EXECUTION_ID:-}" ]; then
    echo "ERROR: RUN_ID and NODE_EXECUTION_ID must be set." >&2
    exit 1
fi

if [ -z "${JOB_SECRET:-}" ]; then
    echo "ERROR: JOB_SECRET not set. This script must be run inside an agent pod." >&2
    exit 1
fi

CONFIG_FILE="/workspace/config.json"
if [ ! -f "$CONFIG_FILE" ]; then
    echo "ERROR: $CONFIG_FILE not found." >&2
    exit 1
fi

# Whether $2 — a branch that exists on origin in the clone at $1 — carries commits
# the repo's default branch does not. $3 is the repo name, for diagnostics.
#
# A branch existing on origin is not the same as this run having pushed work to it.
# The failure safety net (entrypoint.sh's preserve_repo_work) pushes the run branch
# whenever a node fails, including when the working tree is clean, which leaves a
# branch sitting at — or behind — the default branch's tip. No pull request can be
# opened for such a branch: a PR needs commits to propose, and there are none. Gating
# on it demands something impossible and leaves the node with no lawful way to
# finish, so measure what the branch actually adds instead of whether it exists.
#
# Deliberately fails safe: every step that cannot be resolved (no discoverable
# default branch, unfetchable refs, a count git will not produce) is treated as
# "carries work", which keeps the gate on rather than silently waiving it. The clone
# is shallow (entrypoint.sh Step 3 clones --depth 1, then deepens to 200 for the
# default branch and the run branch), so a branch that diverged beyond that window
# counts high rather than low — the same safe direction.
#
# Diagnostics go to stderr: stdout is this script's "<repo>: no PR registered"
# contract, which callers parse.
branch_adds_commits() {
    local repo_dir="$1" branch="$2" repo_name="$3"

    local default_ref default_branch
    default_ref=$(git -C "$repo_dir" ls-remote --symref origin HEAD 2>/dev/null \
        | awk '$1 == "ref:" { print $2; exit }')
    default_branch="${default_ref#refs/heads/}"

    if [ -z "$default_branch" ]; then
        echo "WARNING: could not determine the default branch for $repo_name; treating $branch as carrying work" >&2
        return 0
    fi

    # A node working on the default branch itself has nothing to propose: its commits
    # are already on the branch a PR would target.
    if [ "$default_branch" = "$branch" ]; then
        echo "$repo_name: on the default branch ($default_branch) — no pull request is possible" >&2
        return 1
    fi

    set +e
    git -C "$repo_dir" fetch --quiet origin "$default_branch" "$branch" >/dev/null 2>&1
    local fetch_exit=$?
    set -e
    if [ "$fetch_exit" -ne 0 ]; then
        echo "WARNING: could not fetch $default_branch or $branch for $repo_name; treating $branch as carrying work" >&2
        return 0
    fi

    local ahead
    ahead=$(git -C "$repo_dir" rev-list --count \
        "refs/remotes/origin/$default_branch..refs/remotes/origin/$branch" 2>/dev/null || true)
    case "$ahead" in
        '' | *[!0-9]*)
            echo "WARNING: could not count $branch's commits over $default_branch for $repo_name; treating it as carrying work" >&2
            return 0
            ;;
    esac

    if [ "$ahead" -eq 0 ]; then
        echo "$repo_name: $branch adds no commits over $default_branch — no pull request is possible" >&2
        return 1
    fi
    return 0
}

# --- Step 1: determine which repos this node pushed commits to (Decision 5) ---
# tab-separated (id, name) of repos whose current branch exists on origin.
PUSHED_FILE=$(mktemp)
trap 'rm -f "$PUSHED_FILE"' EXIT

REPOS_LEN=$(jq '(.repos // []) | length' "$CONFIG_FILE")

if [ "$REPOS_LEN" -gt 0 ]; then
    while IFS=$'\t' read -r REPO_ID REPO_NAME REPO_DIR; do
        if [ ! -d "$REPO_DIR" ]; then
            continue
        fi

        BRANCH=$(git -C "$REPO_DIR" rev-parse --abbrev-ref HEAD 2>/dev/null || true)
        if [ -z "$BRANCH" ] || [ "$BRANCH" = "HEAD" ]; then
            continue
        fi

        set +e
        git -C "$REPO_DIR" ls-remote --exit-code --heads origin "$BRANCH" >/dev/null 2>&1
        LS_REMOTE_EXIT=$?
        set -e

        if [ "$LS_REMOTE_EXIT" -eq 0 ]; then
            if branch_adds_commits "$REPO_DIR" "$BRANCH" "$REPO_NAME"; then
                printf '%s\t%s\n' "$REPO_ID" "$REPO_NAME" >>"$PUSHED_FILE"
            fi
        elif [ "$LS_REMOTE_EXIT" -eq 2 ]; then
            continue
        else
            echo "ERROR: could not reach origin for $REPO_NAME (git ls-remote exited $LS_REMOTE_EXIT)" >&2
            exit 1
        fi
    done < <(jq -r '.repos[] | [.id, .name, (.local_path // ("/workspace/repo/" + .name))] | @tsv' "$CONFIG_FILE")
fi

if [ ! -s "$PUSHED_FILE" ]; then
    # Nothing was pushed this run — nothing to gate on.
    exit 0
fi

# --- Step 2: fetch the set of repos that already have a registered PR ---
# Guarded the same way the git ls-remote call above is (set +e/set -e around it):
# under set -euo pipefail, a bare `$(curl ...)` would otherwise let curl's own
# transport-failure exit code (e.g. 6/7/28 for DNS/connect/timeout failures) kill
# this script immediately, with no diagnostic at all (curl -s suppresses curl's own
# stderr message, and the HTTP_CODE check below — which would otherwise produce
# one — is never reached). That would silently defeat Caveat 3's "fail loudly on
# an unreachable dependency" requirement for this second network call, the same
# way it's already honored for the ls-remote call above.
set +e
RESPONSE=$(curl -s -w "\n%{http_code}" \
    -H "Authorization: Bearer $JOB_SECRET" \
    "$API_SERVER_URL/internal/runs/$RUN_ID/node-executions/$NODE_EXECUTION_ID/pull-requests")
CURL_EXIT=$?
set -e

if [ "$CURL_EXIT" -ne 0 ]; then
    echo "ERROR: could not reach $API_SERVER_URL for PR check (curl exited $CURL_EXIT)" >&2
    exit 1
fi

HTTP_CODE=$(echo "$RESPONSE" | tail -1)
BODY=$(echo "$RESPONSE" | sed '$d')

if [ "$HTTP_CODE" != "200" ]; then
    echo "ERROR: PR check failed (HTTP $HTTP_CODE): $BODY" >&2
    exit 1
fi

REGISTERED_REPO_IDS=$(echo "$BODY" | jq -r '.[].gitRepoId')

# --- Step 3: report pushed repos missing a registered PR ---
MISSING=0
while IFS=$'\t' read -r REPO_ID REPO_NAME; do
    if ! grep -qxF "$REPO_ID" <<<"$REGISTERED_REPO_IDS"; then
        echo "$REPO_NAME: no PR registered"
        MISSING=1
    fi
done <"$PUSHED_FILE"

if [ "$MISSING" -eq 1 ]; then
    exit 1
fi

exit 0
